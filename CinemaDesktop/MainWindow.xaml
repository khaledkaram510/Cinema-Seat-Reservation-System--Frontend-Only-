<Window x:Class="CinemaDesktop.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Cinema Booking" Height="720" Width="1100"
        WindowStartupLocation="CenterScreen"
        Background="#0B1220">

	<DockPanel>

		<!-- Top Bar -->
		<Border DockPanel.Dock="Top" Background="#0F1A33" Padding="16">
			<Grid>
				<Grid.ColumnDefinitions>
					<ColumnDefinition Width="*" />
					<ColumnDefinition Width="Auto" />
				</Grid.ColumnDefinitions>

				<StackPanel Orientation="Vertical">
					<TextBlock Text="Cinema Seat Booking"
                               Foreground="White"
                               FontSize="22"
                               FontWeight="Bold"/>
					<TextBlock Text="Functional Core (F#) + WPF Shell (C#)"
                               Foreground="#B7C2E0"
                               FontSize="12"/>
				</StackPanel>

				<StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center" >
					<Button x:Name="CreateHallNavBtn" Content="Create Hall"
                            Width="130" Height="36" Margin="0,0,10,0"
                            Click="CreateHallNavBtn_Click"/>
					<Button x:Name="BookingNavBtn" Content="Booking"
                            Width="130" Height="36"
                            Click="BookingNavBtn_Click"/>
				</StackPanel>
			</Grid>
		</Border>

		<!-- Content -->
		<Frame x:Name="MainFrame" NavigationUIVisibility="Hidden" />

	</DockPanel>
</Window>
